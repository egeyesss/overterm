{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the main terminal window. Opening a link needs both opener entries: allow-open-url permits calling the command, and allow-default-urls carries the scope the URL itself is checked against. With only the first, every link is refused as out of scope. start-resize-dragging is what lets the window be dragged bigger by its edges, which a window with no system frame has to ask for rather than getting from the frame.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "core:window:allow-start-resize-dragging",
    "opener:allow-open-url",
    "opener:allow-default-urls"
  ]
}
